{
  "repo_path": "..",
  "branch": "main",
  "remote": "origin",
  "log_file": "updater/updater.log",
  "lock_file": "updater/updater.lock",
  "skip_if_dirty": true,
  "auto_push_local_ahead": false,
  "stop": {
    "mode": "port",
    "port": 8002
  },
  "start": {
    "windows": "npm start",
    "linux": "PORT=8002 npm start"
  },
  "setup": {
    "enabled": true,
    "windows": "if not exist .venv\\Scripts\\python.exe (python -m venv .venv) && .venv\\Scripts\\python.exe -m pip install -r requirements.txt && npm install",
    "linux": "([ -d .venv ] || python3 -m venv .venv) && . .venv/bin/activate && pip install -r requirements.txt && npm install",
    "run_when_changed": [
      "requirements.txt",
      "package.json",
      "package-lock.json"
    ]
  }
}
